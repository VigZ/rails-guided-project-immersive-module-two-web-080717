<h1>Hello <%= @user.username %>! </h1>
Here are the photos of <%= @user.username %>!

<% @pictures.each do |picture| %>
  <h2><%= picture.title %><h2>
  <%= link_to (image_tag (picture.image_url)), picture_path(picture) %><br>
<% end %>
